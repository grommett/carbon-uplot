/** Dispatched on `document` when the active Carbon theme changes. */
export const THEME_CHANGE_EVENT = 'cu-theme-change';

/** Dispatched on the chart mount element after the chart is recreated following a theme change. */
export const CHART_RECREATED_EVENT = 'cu-chart-recreated';
